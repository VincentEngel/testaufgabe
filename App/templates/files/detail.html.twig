<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}File details{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
{% if msg is defined %}
    <p>{{ msg }}</p>
{% endif %}
<div>Filename: {{ name }}</div>
<div>Filesize: {{ fileSize }}</div>
<div style="margin: 4em;">
    {{ form_start(renameFileForm) }}
    {{ form_row(renameFileForm.name) }}
    <button type="submit" class="btn" style="margin-top: 2em;">Rename</button>
    {{ form_end(renameFileForm) }}
</div>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}
<div>
    <a href="{{ path('files_view') }}">FilesMenu</a>
</div>
</body>
</html>
